= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f|
  %h2 Sign up!
  = devise_error_messages!
  %p
    = f.text_field :email
    = f.label :email
  %p
    = f.text_field :first_name
    = f.label :first_name, "First Name"
  %p
    = f.text_field :last_name
    = f.label :last_name, "Last Name"
  %p
    = f.password_field :password
    = f.label :password
  %p
    = f.password_field :password_confirmation
    = f.label :password_confirmation, "Confirm Password"
  %p 
    = f.submit "Sign up"
  %p
    = render :partial => "devise/shared/links"
#signup-text
  :markdown    
    Again, welcome to ListenUp. We're trying a new way of doing surveys: Simplicity. This means...
    
    * You won't have to deal with options you don't want.
    * It will be easy to send surveys, and the data is available as CSV or an Microsoft Excel Spreadsheet.
    * Just you, your surveys, and your data.

